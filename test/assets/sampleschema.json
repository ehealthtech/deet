{
    "title": "First and Last",
    "type": "object",
    "properties": {
    "firstName": {
        "type": "string",
        "pattern": "^[A-Za-z]+$"
    },
    "lastName": {
        "type": "string"
    },
    "age": {
        "description": "Age in years",
            "type": "integer",
            "minimum": 0
    }
},
    "required": ["firstName", "lastName"]
}